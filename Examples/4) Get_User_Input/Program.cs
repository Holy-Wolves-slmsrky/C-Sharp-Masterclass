
Console.Write("Please Enter Data: ");

/*
 * This code takes the user input and passes the data to the variable (stores the data in the variable).
 * Bu kod kullanıcı girdisini alır ve verileri değişkene aktarır (verileri değişkende saklar).
 */
string userInput = Console.ReadLine();

Console.WriteLine(userInput);

Console.ReadKey();
